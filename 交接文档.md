# LocalEnv-Guard 交接文档

更新时间：2026-07-22（北京时间）

## 当前结论

双语 CLI、发布审查加固和公开 GitHub 部署已完成。GitHub Actions 的 Windows、macOS、Linux 与 Node.js 18、20、22 共 9 项任务已在提交 `e79ee46` 全部通过；该远程矩阵是当前发布资格的有效证据。后续任何提交都必须重新完成同一矩阵，不能沿用本次结果。

## 本次核验结果

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| `npm.cmd run test:ci` | 通过：43 项中 42 项通过、1 项条件跳过 | 覆盖双语 CLI、端口注入、未知进程避让、保护服务熔断、进程树清理、终止前的 PID/命令/启动时间复核，以及跨平台 npm 命令解析。 |
| `npm.cmd run pack:check` | 远程通过：9 项 CI 均完成该检查 | `CI #3` 的 9 个任务均执行并通过同一打包检查。2026-07-22 本机复跑受 npm 缓存临时文件冲突阻断，未使用 `--force` 覆盖缓存；此前已验证发布包为 19 个文件，含 README.en.md 与 src/i18n.js。 |
| `npm.cmd audit --omit=dev` | 通过：0 vulnerabilities | 项目没有运行时依赖，仍保留审计结果作为发布取证。 |
| 双语 CLI | 本地实现完成 | `--lang zh`、`--lang en`、`LEG_LANG` 支持 leg 自身的帮助、端口恢复、交互菜单和 doctor 文案；子进程输出保持子进程原语言。 |

## 历史本地证据（不可直接沿用）

- Windows 本地门禁曾验证 `npm.cmd run test:ci` 为 `34/34` 通过，`npm.cmd run pack:check` 通过，发布包为 17 个文件。
- `34/34` 和 17 文件结果早于当前双语改动，因此只能作为历史参考。中英文 README 与安全性自测报告已随本次实现更新；仍必须重新执行本版本的打包检查。

## 当前运行方式

`leg` 尚未全局安装。Windows 本地入口为：

```powershell
node "D:\workspace\本地环境守卫\bin\leg.js" --auto npm run dev
```

## 发布与验收边界

- 本地首个提交为 `3e42e9d feat: add bilingual CLI support`；公开 GitHub 仓库已创建：`https://github.com/1yang2471/localenv-guard`。本轮审查修复、文档和元数据尚未提交或推送。
- 首次远程运行 `CI #1`（提交 `3280f45`）的 6 个 macOS/Linux 任务失败，原因是 Windows 专属 `npm.cmd` 单测在非 Windows 平台仍强制执行，并非产品运行故障。已改为平台条件测试，并新增非 Windows 的 npm 原命令断言。
- 第二次远程运行 `CI #2`（提交 `664a858`）只在 macOS Node 18 失败：不同进程型集成测试并发运行，夹具互相占用端口，产品按三次避让上限停止。已将 npm 测试命令固定为 `--test-concurrency=1`；修复后的提交尚待远程矩阵验证。
- 先前 GitHub CLI 的网页授权可用，但命令行 HTTPS/API 网络问题阻断了授权流程；本次未重新验证该外部状态。
- 真实 Vite 手工 E2E 曾多次受到残留进程和操作顺序干扰，不能作为验收证据。当前有效的 Windows 依据应以自动化测试为准；测试不全绿时也不构成有效发布证据。
- GitHub Actions `CI #3`（Run ID `29913479552`，提交 `e79ee46`）状态为 `Success`，明确显示 `9 jobs completed`，覆盖 Windows、macOS、Linux 与 Node.js 18、20、22。该提交满足部署验收门槛；未执行 npm 发布或创建 Git tag。

## 后续顺序

1. 网络恢复后执行 `git fetch origin`，核验网页提交 `e79ee46` 与本地提交 `773b430` 的共同父提交和文件树，再用非破坏方式同步本地 `origin/main`。
2. 任何源代码、文档或工作流变更推送后，都重新等待 9 项 GitHub Actions 任务通过；不要把 `CI #3` 沿用到新提交。
