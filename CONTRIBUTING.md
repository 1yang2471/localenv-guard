# 贡献指南

## 本地检查

```bash
npm test
npm run pack:check
```

涉及终止逻辑、端口发现或保护服务名单的修改，必须新增行为测试，并在 Windows、macOS、Linux 三个平台 CI 通过后再合并。

## 安全边界

- 不降低受保护服务、高权限账户和系统服务的熔断规则。
- 不新增静默终止行为；终止普通进程必须由交互确认触发。
- 进程查询失败时只能换端口或停止，不能猜测进程类型后终止。
