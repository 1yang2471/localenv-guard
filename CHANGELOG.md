# 改动日志

## 2026-07-22

- 修复 `leg --help` 与 `leg -h`：现在显示用法，不再把帮助参数当作启动命令执行。
- 未知进程避让自动化验收新增“占用者仍存活”和“不输出终止 PID”的断言。
- 新增 `npm.cmd run test:acceptance`，一键执行核心 Windows 验收集。
- 新增 `交接文档.md`，记录本地证据、当前失败项、环境阻断和 GitHub 发布门禁，避免将历史 `34/34` 误作当前发布结论。
- 完成双语 CLI：`--lang zh`、`--lang en` 与 `LEG_LANG` 可控制 leg 自身的帮助、端口恢复提示、交互菜单和 `doctor` 输出；命令行参数优先于环境变量。
- 新增英文 README，并更新安全性自测报告与 npm 发布白名单。
- 本次完整门禁为 `40/40` 通过；远程 9 项 CI 尚未执行，项目仍不能发布。
- 本轮 `npm.cmd run pack:check` 通过，`localenv-guard-0.1.0.tgz` 发布包为 19 个文件。

## 2026-07-21

- 创建 LocalEnv-Guard 原型项目，目录为 `D:\workspace\本地环境守卫`。
- 实现 `leg <命令>` 包装启动、端口冲突识别、占用者查询、交互决策、换端口重试和进程树终止。
- 实现 `leg doctor` 常见开发端口体检。
- 增加保护服务熔断名单，避免误杀数据库、中间件和系统级服务。
- 增加 Node 原生测试，覆盖端口解析、端口注入、保护服务判断和 doctor 建议动作。
- 增加 Windows 命令解析，避免 `shell: true` 破坏 `node -e` 等参数并降低 shell 拼接风险。
- 增加 Windows `netstat -ano` / `Get-Process` 兜底，当前环境可显示端口占用者 PID 和进程名。
- 增加真实端口冲突冒烟夹具，验证冲突后自动换端口重试。
- 编写 README 与安全性自测报告，说明当前能力和边界。
- 安全加固：将 root/Windows 系统账户、Windows 核心服务宿主和 Unix 初始化服务纳入熔断范围，杜绝向这些进程提供终止选项。
- 异常加固：端口恢复过程失败时返回原命令退出码并输出中文提示，避免 CLI 暴露 Node 异常堆栈。
- 增加高权限进程、Windows 系统服务与端口恢复失败回归测试；完整测试增至 18 项。
- 补充占用者画像中的启动时间或运行时长显示，并完成一次 Windows 真实端口冲突恢复回归。
- 发布准备：补充 MIT 许可证、安全披露策略、贡献指南、npm 打包白名单和 Windows/macOS/Linux CI 矩阵。
- 终止强化：Windows 在 taskkill 被拒绝时回退到原生进程快照递归清理；Unix 在 SIGTERM 超时后使用 SIGKILL 兜底。
- 安全强化：同一端口存在多个监听进程时，只要任一进程受保护就整体熔断。
- 新增真实进程树回归，验证忽略 SIGTERM 的子进程也会被清理且端口最终释放；当前测试总数为 20 项。
- 进一步收紧终止权限：仅允许明确识别为本地开发服务器命令的进程进入终止流程；未知进程在 `leg` 与 `leg doctor` 中统一降级为手动处理。
- 增加未知服务拒绝终止、明确开发命令允许终止的回归测试；当前测试总数为 22 项。
- 增加冲突菜单回归测试，确保未知进程在交互界面中不出现终止选项；当前测试总数为 23 项。
- 第二阶段安全打磨：扩展核心服务熔断与开发框架识别，保护/开发命令回归集覆盖 82 个案例，且保护服务始终优先匹配。
- 增加 `leg --auto <命令>`：明确开发进程自动清理并重试，未知进程自动换端口，保护服务仍中断。
- Windows 修复：npm.cmd 改由官方 npm-cli.js 启动，避免 shell:false 下的 spawn EINVAL 且不引入 shell 参数解释。
- 端口覆写强化：npm run 脚本自动追加 `-- --port <新端口>`；新增严格 Vite/Django 语义夹具和 npm 脚本集成测试。
- 当前测试总数为 33 项。
